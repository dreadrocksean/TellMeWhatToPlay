import { StyleSheet } from "react-native";

export default StyleSheet.create({
  body: {
    flex: 1,
    justifyContent: "center"
  },
  h1: {
    fontSize: 16,
    color: "white",
    fontWeight: "bold",
    textAlign: "left"
  }
});
